export interface Description {
  outerDesc: string;
  innerDesc: string;
  background: string;
  notes: string;
  belief: string;
  events: string;
  backpack: string;
  backpackContents: number;
  backpackSlots: number;
}
